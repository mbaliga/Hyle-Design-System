{
  "name": "hyle-design-system",
  "version": "0.1.0",
  "description": "Cross-platform design system — one set of tokens for web, Android, and iOS, with Lit web components and Storybook docs.",
  "type": "module",
  "license": "MIT",
  "files": [
    "build",
    "src"
  ],
  "exports": {
    ".": "./src/components/index.ts",
    "./tokens.css": "./build/web/tokens.css",
    "./tokens": "./build/web/tokens.js"
  },
  "scripts": {
    "tokens:build": "node scripts/build-tokens.js",
    "picker:build": "node scripts/build-color-picker.js",
    "surface:build": "node scripts/build-texture-surface.js",
    "assets:build": "npm run tokens:build && npm run picker:build && npm run surface:build",
    "prestorybook": "npm run assets:build",
    "storybook": "storybook dev -p 6006 --no-open",
    "prebuild-storybook": "npm run assets:build",
    "build-storybook": "storybook build",
    "build": "npm run assets:build",
    "format": "prettier --write \"**/*.{ts,js,json,css,md}\""
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.2.8",
    "@fontsource-variable/jetbrains-mono": "^5.2.8",
    "chroma-js": "^3.2.0",
    "iwanthue": "^2.0.0",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@storybook/addon-essentials": "^8.6.14",
    "@storybook/blocks": "^8.6.14",
    "@storybook/web-components-vite": "^8.6.14",
    "@types/chroma-js": "^3.1.2",
    "prettier": "^3.4.2",
    "storybook": "^8.6.14",
    "style-dictionary": "^4.3.0",
    "typescript": "^5.7.3",
    "vite": "^5.4.11"
  }
}
